'use client';

import Image from 'next/image';

const NavIcons = () => {
  return (
    <div>
      <Image src="/profile.png" alt="" width={22} height={22} />
    </div>
  );
};

export default NavIcons;
